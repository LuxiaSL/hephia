id: interfaces.exo.memory

defaults:
  snippet: |
    Command: ${command_input}
    Response: ${response}
  
  combined: |
    Review the recent terminal interaction history and form a clear first-person memory of what occurred:

    Recent Interaction History:
    ${interaction_history}

    Additional Context:
    ${context}

    Focus on what was meaningful, any changes in understanding, significant events, or patterns that emerged from these interactions. Form this as a coherent memory from your perspective.